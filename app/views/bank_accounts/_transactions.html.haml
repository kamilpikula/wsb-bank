%table.table.table-bordered
  %thead
    %tr
      %th Data transakcji
      %th Typ transakcji
      %th Kwota
  %tbody
    - bank_account.account_transactions.order(id: :desc).each do |t|
      %tr
        %td #{t.created_at.strftime('%d.%m.%Y %H:%M:%S')}
        %td #{t.transaction_type}
        %td #{number_to_currency(t.amount, unit: "PLN")}
    
    %tr
      %th
      %th Bilans
      %th #{number_to_currency(bank_account.balance, unit: "PLN")}